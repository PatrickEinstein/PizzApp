import React from 'react';
import { View } from 'react-native';

const HDivider = () => {
  return (
    <View
      style={{
        borderBottomColor: 'black',
        borderBottomWidth: 1,
      }}
    />
  );
};

export default HDivider;